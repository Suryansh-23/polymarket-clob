# Environment variables for Polymarket CLOB Sequencer

# Ethereum RPC endpoint (required)
RPC_URL=http://localhost:8545

# Private key for transaction signing (required - replace with your key)
PRIVATE_KEY=your_private_key_here

# BatchSettlement contract address (update after deployment)
BATCH_SETTLEMENT_ADDRESS=0x5FbDB2315678afecb367f032d93F642f64180aa3

# Optional: Gas price in wei (default: suggested gas price from network)
# GAS_PRICE=20000000000

# Optional: Gas limit for batch submission (default: 500000)
# GAS_LIMIT=500000
